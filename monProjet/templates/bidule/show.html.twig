{% extends 'base.html.twig' %}

{% block title %}Bidule{% endblock %}

{% block body %}
    <h1>Bidule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bidule.id }}</td>
            </tr>
            <tr>
                <th>Chouette</th>
                <td>{{ bidule.chouette }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bidule_index') }}">back to list</a>

    <a href="{{ path('bidule_edit', {'id': bidule.id}) }}">edit</a>

    {{ include('bidule/_delete_form.html.twig') }}
{% endblock %}
