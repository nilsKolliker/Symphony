<form method="post" action="{{ path('bidule_delete', {'id': bidule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bidule.id) }}">
    <button class="btn">Delete</button>
</form>
